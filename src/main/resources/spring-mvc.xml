<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- 扫描Controller -->
    <!-- 1. 扫描 Controller 包：让 Spring 找到所有 Controller -->
    <context:component-scan base-package="com.attendance.controller"/>

    <!-- 开启MVC注解 -->
    <!-- 2. 开启 MVC 注解驱动：支持 @GetMapping、@PostMapping 等 -->
    <mvc:annotation-driven/>

    <!-- 静态资源映射 -->
    <!-- 4. 静态资源映射：让图片、CSS、JS 能被访问 -->
    <mvc:resources mapping="/static/**" location="/static/"/>
    <mvc:default-servlet-handler/>

    <!-- 视图解析器 -->
    <!-- 3. 视图解析器：把"login"变成"/WEB-INF/views/login.jsp" -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
        <property name="contentType" value="text/html;charset=UTF-8"/>
    </bean>



</beans>
